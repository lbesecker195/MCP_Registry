#!/bin/sh
set -eu

# Submits every listing to IndexNow and pings the WebSub hubs, from inside the
# running node. A one-off backfill; each sync announces its own changes.
cd -P -- "$(dirname -- "$0")"
exec ./mcp_registry rpc "IO.inspect(McpRegistry.Discovery.announce_all())"
